# NexaCloud Kernel

The Linux-libre kernel source code belongs in this directory.

Due to the size of the repository, it could not be cloned automatically. To continue with the build process, please clone it manually:

```bash
git clone --depth=1 https://github.com/librelinux/librelinux.git .
```
